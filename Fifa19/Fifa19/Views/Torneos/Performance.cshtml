@model IEnumerable<Fifa19.Models.sp_desempenhoArbitroTorneo_Result>

@{
    ViewBag.Title = "Performance";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h4>"          "</h4>
<h2>Referee Performance</h2>
<table class="table">
    <tr>
        <th>
            Nombre
        </th>
        <th>
            Porcentaje de Desempeño
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Column1.Value)
            </td>
        </tr>
}

</table>

@Html.ActionLink("Back to List", "Index")
