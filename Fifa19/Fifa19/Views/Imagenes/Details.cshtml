@model Fifa19.Models.Imagenes

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Imagenes</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nombre)
        </dt>
        <dt>
            @Html.DisplayNameFor(model => model.image_byte)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre)
        </dd>
        <dd>
            <video controls>
                <source src="data:image;base64,@System.Convert.ToBase64String(Model.image_byte)" type="video/mp4" />
                Your browser does not support the video tag.
            </video>
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.nombre }) |
    @Html.ActionLink("Back to List", "Index")
</p>
